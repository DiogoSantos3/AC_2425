/*

4 -> É recomendado armazenar variáveis não temporárias no stack e usar os registos
 temporários (como r0, r1, r2, r3) para armazenar valores temporários. Isso é importante 
 porque, ao chamar outras funções, essas funções podem modificar os registos temporários 
 (r0 a r3), o que pode causar alterações indesejadas. Portanto, para garantir que os valores
  não sejam sobrescritos, é mais seguro armazenar variáveis não temporárias no stack.
  Portanto a resposta certa é no r5

*/
